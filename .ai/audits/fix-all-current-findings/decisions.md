# 待决策事项

当前没有阻塞代码修复的需求歧义。

真实刷写前仍需由设备所有者确认：设备运行配套 multi-layout U-Boot、选择 `x1pro-mod-112m`，且保存环境不强制只读取旧 `fit` 卷。这是外部状态核验，不是仓库内设计决策。

孤立 Tag 清理保留 Release 快照方案。GitHub 没有把“确认不存在 Release”和“删除 Tag”合并为原子操作的接口，逐 Tag 复查也只能缩短而不能消除竞态，还会引入把 404 与 API/权限故障可靠区分的额外分支。当前范围已限制为 `X1Pro-*`，且任何删除失败都会让任务失败；将剩余窄窗口记录为 F-023/P3 平台风险。
